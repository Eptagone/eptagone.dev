---
import type { LayoutProps } from "@/shared/models";
import Layout from "./LegalLayout.astro";

interface Props {
    frontMatter: LayoutProps;
}
---

<Layout {...Astro.props.frontMatter}>
    <slot />
</Layout>
